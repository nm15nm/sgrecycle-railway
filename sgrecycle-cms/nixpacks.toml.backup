[phases.build]
cmds = [
    "npm install --frozen-lockfile --no-optional --no-fund --cache /tmp/npm-cache",
    "npm run build"
]

[phases.start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
NPM_CONFIG_CACHE = "/tmp/npm-cache"
